import AmenityChip from "@/components/bookinginterfacecomponent/basicdetailsComponents/vechileselectcomponents/AmenityChip";

// Kept for backwards compatibility
const AmenityIcon = ({ name }) => <AmenityChip name={name} />;

export default AmenityIcon;
